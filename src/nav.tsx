import classes from './nav.module.css'

const Nav = ()=> {
return (
    <header>
    <nav className={classes.nav}>
<h3>Is this word a Palindrome?</h3>
    </nav>
    </header>
)
}

export default Nav;